#include "main.h"
#include <stdio.h>
/**
 *set_string-a function that sets the value of a pointer to a char.
 *@to:arry of num_ber________
 *@s:arry 1_______
 * Return: returns always (0) ______
 */

void set_string(char **s, char *to)
{
*s = to;
}
