#include "main.h"

/**
 *_isdigit-  a function that checks for a digit (0 through 9).
 * @form: number to check
 * Return: 1 if from is true or 0 if it false
 */

int _isdigit(int form)
{

if (form >= 48  &&  form <= 57)
return (1);
else
return (0);


}
