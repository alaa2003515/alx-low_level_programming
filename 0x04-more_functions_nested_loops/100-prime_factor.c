
#include <stdio.h>
/**
 * main - a program and prints the largest prime factor of the number
 * Return: A_l_w_a_y_s (0).
 */

int main(void)
{
long alaa;
long int lol = 612852475143;
long lolo;
double lo = sqrt(lol);

for (alaa = 1; alaa <= lo; alaa++)
{
if (lol % alaa == 0)
{
lolo = lol / alaa;
}
}
printf("%ld\n", lolo);
return (0);
}
