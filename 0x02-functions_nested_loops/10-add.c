#include "main.h"

/**
 *  add - returns the result of adds two integers
 * Description : a function that adds two integers and returns the result
 * @wr: number 1
 * @tr: number to 2
 * Return: Always the result (Success)
 */


int add(int tr, int wr)
{

int oop = wr + tr;
return (opp);

}
