
#include <stdio.h>

/**
 * main-program  prints name of the file it was compiled,followed by a new line
 * Return: returns always (0) ______
 */

int main(void)
{

printf("%s\n", __FILE__);

return (0);
}
