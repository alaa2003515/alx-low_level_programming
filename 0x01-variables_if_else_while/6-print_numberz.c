#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */


int main(void)
{
int var = 0;

for (var = 0 ; var <= 10 ; var++)
{
putchar(var);
}
putchar('\n');
return (0);
}

