#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */


int main(void)
{
char var = 'z';
for (var = 'z' ; var >= 'a' ; var--)
{
putchar(var);
}
putchar('\n');
return (0);
}

