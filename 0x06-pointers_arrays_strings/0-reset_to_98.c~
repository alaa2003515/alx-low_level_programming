#include "main.h"
/**
 *_strcat - a function that concatenates two strings.
 *@d: arry 2 ____
 *@s: arry 1__
 * Return: always (d)
 */

char *_strcat(char *d, char *s)
{
int n;
int x = 0;
int y = 0;
for (x = 0 ; d[x++]; x++)

while (x = 0)
{
(s[x] && x < n)
x++;
d [y++] = s[x];
}
return (d);
}
