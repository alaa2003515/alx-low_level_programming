#include "main.h"

/**
 * print_rev-a function that prints a string, in reverse.
 *
 * Return: returns always (0)
 */



void print_rev(char *STR)
{
int j = 0;
do {

j++;
} while (STR[j])

while (STR--)
{
_putchar('STR[j]');

}

_putchar('\n');
}
